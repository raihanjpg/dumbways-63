<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> MyProfile </title>
    <style>
        body{
            font-family: Arial, Helvetica, sans-serif;
            background: #f3f4f6;
            display: flex;
            justify-content:center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .card{
            background: white;
            padding: 20px 30px;
            border-radius: 10px;
            text-align: center;
            width: 300px;
        }
        .card img{
            border-radius: 50%;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>Identitas Diri</h1>
        <img src="{{photo}}" alt="Foto {{name}}" width="120" height="120">
        <p><strong>Nama:</strong> {{name}}</p>
        <p><strong>Umur:</strong>{{age}} Tahun</p>
        <p><strong>Alamat:</strong>{{address}}</p>
        <p><strong>Email:</strong>{{email}}</p>
    </div>
</body> 
</html>